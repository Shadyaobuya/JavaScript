function fruits(){          //declare a function called fruits
    let fruits=["avocado", "grapes","mangoes", "orange", "kiwi"];   //store the fruits in an array called fruits
    for(i in fruits){     //use a for loop to loop through every item in the array
        console.log(fruits[i]);  //print all the items in the array
}
}
fruits();   //call function fruits

